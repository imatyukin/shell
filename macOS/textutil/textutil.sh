#!/usr/bin/env bash
# Конвертация текстовых файлов

# Конвертация в файл Word
# (DOCX, DOC, RTF, HTML, ODT или WebArchive (веб-страница в формате Safari))
# textutil -convert txt <файл.docx>

# Массовая конвертация файлов
# (в любом направлении, без сохранения форматирования)
# textutil -convert txt ~/Desktop/*.docx
